/*import { defineStore } from 'pinia';
import { BuildingInterface } from '../script/interface';

export const BuildingStore = defineStore({
  id: 'Building',
  state: () => ({
    Buildings: {} as BuildingInterface,
  }),
  actions: {
    setBuilding (buildings:BuildingInterface) {
      this.Buildings = buildings;
    },
  },
});*/